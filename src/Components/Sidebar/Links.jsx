import React from "react";

const Links = () => {
  const sidebarItems = ["Home", "Services", "Contact", "Portfolio", "About"];
  return <div>{sidebarItems.map((item, index) => {})}</div>;
};

export default Links;
